apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  namespace: monitoring-project-dev
spec:
  selector:
    app: nginx 
  ports:
    - protocol: TCP
      port: 80      
      targetPort: 80 
  type: ClusterIP  
